# Backend environment example (copy to backend/src/.env)
# Server port; the server will also auto-fallback to the next free ports if needed
PORT=4000

# MongoDB connection string (Atlas or local)
# For Atlas SRV URIs, ensure your DNS can resolve the host
MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster-host>/<db>?retryWrites=true&w=majority

# Allow web/mobile apps to send cookies/credentials
# In dev you can set this to * or to your Expo dev URL
CLIENT_ORIGIN=*
